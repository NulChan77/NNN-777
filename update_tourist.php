<?php
if ($_SERVER['REQUEST_METHOD'] == 'POST') {
    $tourist_id = $_POST['tourist_id'];
    $title = $_POST['title'];
    $image_path = ''; // เพิ่มการจัดการอัปโหลดรูปภาพหากจำเป็น

    // เพิ่มโค้ดสำหรับเชื่อมต่อฐานข้อมูล และอัปเดตข้อมูล
    // เช่น บันทึกชื่อและที่อยู่รูปภาพในฐานข้อมูล

    echo 'บันทึกข้อมูลสำเร็จ';
} else {
    http_response_code(405);
    echo 'ไม่อนุญาตให้เข้าถึงวิธีการนี้';
}